import React from "react";

import classes from "./Navbar.module.css";

const Navbar = () => {
  return (
    <div className={classes["nav-bar"]}>
      <h2 className={classes.h2}>Employee1</h2>
    </div>
  );
};

export default Navbar;
